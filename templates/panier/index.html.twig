{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}

    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Votre panier</h1>

        <p>Ici le contenu du panier</p>





    </div>
    {{ dump(panierService) }}

    {#
    {% if panierService.fullPanier | length > 0 %}
        <thead>
        <tr>
            <th>Produit</th>
            <th>Prix</th>
            <th>Quantité</th>
            <th>Total</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>{{ panierService }}</td>
            <td>{{ panierService.produit.price }}</td>
            <td>{{ panierService.quantity }}</td>
            <td>{{ panierService.produit.price * panierService.quantity }}</td>
            <td><a href="{{ path('panier_remove', {'id': panierService.produit.id}) }}" class="btn btn-danger btn-sm">
                    <i class="fas fa-trash"></i>
                </a>
            </td>
        </tr>
        </tbody>
        <tfoot>
        <tr>
            <td colspan="3" class="text-right" > Total : </td>
            <td>{{ total }}</td>
            <td></td>
        </tr>
        </tfoot>


    {% else  %}
        <p>Panier vide :(</p>
    {% endif %}
    #}
{% endblock %}
